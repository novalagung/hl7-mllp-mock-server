services:
  hl7-ack:
    container_name: hl7-mllp-mock-server
    image: novalagung/hl7-mllp-mock-server:latest
    environment:
      # HOST: "0.0.0.0"
      ACK_PORT: 2575
      CHAOS_PORT: 2576
    ports:
      - "2575:2575"
      - "2576:2576"
    restart: unless-stopped
